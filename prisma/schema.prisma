generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model alunos {
  id      Int     @id @default(autoincrement())
  nome    String
  turma   String
  materia String
  foto    String?

  @@map("alunos")
}
